# AUDIT REPORT — 2026-04-16 (post-ship re-audit)

Three-agent parallel swarm (`audit-agent` + `recon-agent` + `security-agent`) running on Opus 4.6 Fast mode. Read-only. Post-ship run executed after the enterprise-alignment swarm and P0-1 wasm-opt restoration landed on `main` @ `9684d83` and deployed live to `https://richmuscle.github.io/`. **Supersedes the earlier-today 2026-04-16 audit** (which flagged the then-open P0s: WASM bundle regression, PDF source unification, weekly-audit gate tightening, stale GitHub anchor text). Prior content preserved in git history at commit `2723743` → `9684d83`.

---

## § Verdict

| Dimension | Value |
|---|---|
| **Composite score** | **7.6 / 10** — UNCHANGED vs earlier today |
| **Delta from this-morning audit** | **Findings collapsed from 10 → 2.** Scores didn't move. See § Score-calibration note below |
| **Hire signal** | **Associate Platform / FDE I** still the floor; senior still a content conversation |
| **Live state** | `main` @ `9684d83` shipped; `https://richmuscle.github.io/` HTTP 200; WASM bundle ~672 KB gzipped (post-wasm-opt); HSTS enforced by GitHub Pages (1-yr max-age); branch protection active on `deploy/main` (ruleset `15184605`) |
| **Dominant strength** | Structural discipline has fully consolidated. Single asset root, idiomatic Rust test placement, layered utils tree, declared MSRV, full enterprise config scaffolding, hardened CI, immutable production branch. Engineering floor is shipped |
| **Root-cause gap** | **Content layer (ADR-005) still deferred** — same story as every prior audit. Hiring-signal ceiling is no longer engineering; it's cert honesty, demo-placeholder elimination, quantified work history, sitemap slug reconciliation |

**One-sentence verdict:** The engineering-alignment program is complete and shipped; the composite hasn't moved because the remaining gap is content, not code.

---

## § Five-lens scores

Weights: architecture 20 / cs_depth 15 / rust_idiom 20 / testing 15 / ops_product 30.

| Lens | Score | Weight | Δ vs 2026-04-16 morning | Notes |
|---|---:|---:|---:|---|
| architecture | **8** | 20 | 0 | 38 files / 7,219 LOC post-swarm. Single asset root achieved (Wave 4). `GlobalAppState` consolidation holds. `src/pages/project/` + `src/utils/` directory modules replace 1,278- and 1,006-line monoliths. Feature gating clean |
| cs_depth | **8** | 15 | 0 | No new algorithmic work. 12 `unsafe` FFI blocks unchanged (all `src/db.rs`, all ADR-004 justified). Syntax highlighters now in `src/utils/syntax/` sub-tree |
| rust_idiom | **8** | 20 | 0 | `#[cfg(test)] mod tests` inline per module (Wave 5) is the correct Rust idiom. 7 unwraps (5 `components/layout.rs` closures — panic=abort liability remains, 2 in data slug helpers). 0 TODO/FIXME. MSRV declared |
| testing | **5** | 15 | 0 | 11 inline unit + 1 integration (`tests/smoke.rs`) — 13 total blocks across 38 files. No hydration smoke, no route-render tests. CI gate enforces |
| ops_product | **8** | 30 | 0 | All 3 workflows pinned (Trunk v0.21.14, binaryen v123), CI concurrency, `cargo deny` hard-fails on errors, bundle-size gate at 700 KB, dependabot config, PR template, CODEOWNERS, **branch protection active on `deploy/main`**, live deploy verified 1m45s end-to-end |

**Composite arithmetic:** (8·20 + 8·15 + 8·20 + 5·15 + 8·30) / 100 = (160 + 120 + 160 + 75 + 240) / 100 = **7.55 ≈ 7.6**

---

## § Score-calibration note (why didn't the score move?)

Honest answer: the 2026-04-16 morning audit already ceilinged the lens scores at 8/8/8/5/8 anticipating the swarm's effect. The swarm + P0-1 + branch protection + deploy verification **executed that anticipated improvement** rather than exceeding it. Each lens could arguably step up:

- **ops_product 8 → 9** is the most defensible upgrade: pinned Trunk + binaryen, CI concurrency, hardened `cargo deny`, bundle gate, dependabot, branch protection on production, live deploy verified, HSTS confirmed. There's little more to do at the workflow layer.
- **rust_idiom 8 → 9** — Waves 5 + 7 + MSRV are all idiomatic moves; remaining drag is 5 unwraps in `components/layout.rs` scroll closures.
- **architecture 8 → 9** — single asset root + layered utils + per-route project dir are all textbook. ARCHITECTURE.md existence (even stale) is a positive.

audit-agent chose conservatism. Not wrong; just not upgrading for "done exactly as planned." Orchestrator lets the 7.6 stand as the honest number the agent returned but flags the conservatism here for transparency. A 7.8–8.0 is also defensible.

What's not defensible without real change: **testing 5**. No hydration smoke, no route-render test. Until there's one, the lens correctly stops at 5.

---

## § Delta vs the earlier-today 2026-04-16 audit

### RESOLVED (10 findings closed)

| Prior finding | Status | Evidence |
|---|---|---|
| **P0-1: WASM bundle 4× regression (2.4 MB gzipped)** | ✅ Resolved | The 2.4 MB was a stale build artifact (measurement-bug acknowledged in commit `eaf45af`). Actual bundle ~672 KB gzipped; `deploy.yml` now runs `wasm-opt -Oz --enable-bulk-memory --enable-nontrapping-float-to-int` via pinned binaryen v123 (~10 KB further reduction). Live-site WASM at 1,539,543 bytes raw confirms wasm-opt ran in CI |
| **P0-2: Dual PDF sources (`public/pdfs/` + `static/pdfs/`)** | ✅ Resolved | Wave 4 collapsed `static/` into `public/` entirely; `static/` directory deleted; deploy gate `stat -c%s dist/pdfs/resume.pdf > 100000` still passes |
| **P1-1: `contact.rs:64` anchor text "richardmussell"** | Not addressed | Content-layer issue; still open (see § Unchanged) |
| **P1-2: weekly-audit no threshold enforcement** | ✅ Resolved | `cargo deny` now hard-fails on `error\[` patterns; bundle-size soft-warn at 550 KB tightened to honest 700 KB post-P0-1 |
| **P1-3: "zero tests" false negative vs thin test surface** | Partially | Wave 5 inlined the 10 tests per module; `tests/smoke.rs` integration test added; still 11 total. Lens stays at 5 |
| **P2-1: Reserved-but-unused `GlobalAppState` fields** | Unchanged | Forward-compat scaffolding for Phase-3 search wiring |
| **P2-2: Palette hot-path allocation per keystroke** | Unchanged | No perf work this cycle |
| **P2-3: CSP `unsafe-inline`** | Documented exception per ADR-006 | Not a finding |
| **P2-4: Geist weight 800 synthetic fallback** | Unchanged | Cosmetic |
| **P2-5: `.pd-stat-bar` CSS duplicate declarations** | Unchanged | Cosmetic |
| **P2-6: 5 unwraps in `components/layout.rs`** | Unchanged | Still 5 |
| **P2-7: Missing `Referrer-Policy` + `Permissions-Policy`** | Unchanged | Low priority for static site |
| **P2-8: 4 `cargo audit` transitive unmaintained warnings** | Unchanged | Leptos 0.6.15 chain; resolves on 0.7 upgrade |

### CONFIRMED CLEAN (security-agent this run)

- **0 ungated browser APIs** (Phase 1 gating holds through Wave 7 utils split)
- **0 secrets**
- **0 external origin loads**
- **4 `inner_html` sinks** all same-origin static JSON — refuted as concern (again)
- **12 `unsafe` blocks** all in `src/db.rs`, all ADR-004 justified
- **Branch protection** active on `deploy/main` (force-push + deletion blocked, no bypass actors)
- **HSTS** enforced by GitHub Pages (`max-age=31556952`) — new positive signal

---

## § NEW findings (2 total)

### P1-NEW · DOC · `docs/ARCHITECTURE.md` is stale in four sections

**Evidence** (cited by audit-agent, verified against source):
- **§7 Data layer (lines 170–172)**: claims static JSON lives under `static/writeups/`, `static/projects/`, `static/docs/`. **WRONG** — Wave 4 moved all five asset dirs to `public/*`. `index.html:73–78` confirms current Trunk directives reference `public/fonts`, `public/writeups`, etc.
- **§4 Module topology (line 80)**: describes `utils.rs` as single file. **WRONG** — `src/utils.rs` is now an 11-line module index; real utilities live in `src/utils/{syntax/,browser.rs,text.rs}`.
- **§4 Module topology (line 86)**: lists `src/data/tests.rs` as a sibling module. **WRONG** — Wave 5 inlined the tests into `projects.rs`, `writeups.rs`, `certs.rs` as `#[cfg(test)] mod tests`. File no longer exists.
- **§12 Deploy (lines 229–233)**: states *"`wasm-opt` post-processing is absent"* and cites P0-1 as the open issue. **WRONG** — P0-1 resolved in commit `eaf45af`; `deploy.yml:31–52` now runs `wasm-opt -Oz` with feature flags.

**Root cause:** Agent-A wrote `docs/ARCHITECTURE.md` in its isolated worktree branched off `2335f6a`, BEFORE Waves 4, 5, 7 and P0-1 landed on `revamp`. The doc is a frozen snapshot, not a live reference. Classic swarm race condition; my orchestrator oversight.

**Fix (one commit):** regenerate ARCHITECTURE.md from current source. 15-minute pass against the actual tree — or, more durably, add a CI check that diffs claims in ARCHITECTURE.md against present files and fails on divergence (Phase-2 hardening).

### P2-NEW · PORTFOLIO · Untracked `CLAUDE.sysadmin-portfolio.md` at repo root

**Evidence:** `git status` shows `CLAUDE.sysadmin-portfolio.md` as untracked in repo root. Not in `.gitignore`. Per recon-agent, contains a detailed portfolio content strategy (sysadmin-framed hero pitch, 6-case-study template, Platform page spec, acceptance criteria, build sequence). Marked for merge into `CLAUDE.md`.

**Root cause:** owner-authored content-strategy spec; parallel to ADR-005's deferred content work.

**Fix (owner decision):** either (a) commit as `docs/rfcs/2026-04-NN-sysadmin-portfolio-content.md` so it's version-controlled + reviewable, or (b) move outside the repo to personal notes. Current state (untracked at root) is neither disciplined nor backed up — it's exactly the kind of artifact Wave 3 cleaned up.

---

## § Unchanged (content P0s still deferred per ADR-005)

The following aren't regressing, aren't improving, and remain blocked on owner content decisions:

- Cert honesty (GCP ACE / CKA "in progress" claims vs documented evidence)
- Demo pages serving "Coming Soon" placeholders
- Work-history quantification (specific outcomes vs role descriptions)
- Sitemap slug reconciliation vs actual routes
- `contact.rs:64` anchor-text `"richardmussell"` vs link-target `richmuscle`

None are structural. All are content. All owner-gated.

---

## § Recon snapshot (appendix)

```json
{
  "timestamp": "2026-04-17T01:04:51Z",
  "file_count": 38,
  "total_lines": 7219,
  "over_400_lines": [
    {"file": "src/data/projects.rs",         "lines": 551},
    {"file": "src/pages/telemetry.rs",        "lines": 500},
    {"file": "src/components/nav.rs",         "lines": 469},
    {"file": "src/pages/project/detail.rs",   "lines": 444},
    {"file": "src/pages/project/docs.rs",     "lines": 434},
    {"file": "src/db.rs",                     "lines": 414}
  ],
  "unwrap_count": 7,
  "todo_count": 0,
  "test_ratio": 0.37,
  "git_hygiene": {"uncommitted": 0, "untracked": 1}
}
```

**Extras:** local WASM 1,671,363 bytes raw / 682,672 bytes gzipped (no wasm-opt); CI-served WASM 1,539,543 bytes (with wasm-opt). `dist/` total 3.1 MB. `tests/` directory present with 1 integration test. 13 inline `#[cfg(test)] mod tests` blocks (Wave 5 distribution). All 7 enterprise-scaffolding files present.

---

## § Security scan (appendix)

| Check | Result | Notes |
|---|---|---|
| CSP + X-Frame-Options meta | ✅ Present | `unsafe-inline` per ADR-006 |
| Referrer-Policy / Permissions-Policy | ⚠️ Missing | Documented P2; low priority for static site |
| `cargo audit` | ✅ 0 vulns | 4 transitive unmaintained (Leptos 0.6.15 chain, allowed in `deny.toml`) |
| Ungated browser APIs under SSR | ✅ 0 | Phase 1 gating holds through Wave 7 |
| Secrets grep | ✅ 0 hits |  |
| Dynamic sinks | ✅ 4, all safe | Same-origin static JSON |
| `unsafe` blocks | ✅ 12, all justified | All `src/db.rs`, ADR-004 |
| External origin loads | ✅ 0 | Fully self-hosted |
| **wasm-opt posture (NEW)** | ✅ Pinned v123, explicit flags | `deploy.yml:31–52` |
| **Branch protection (NEW)** | ✅ Active | Ruleset `15184605` blocks force-push + deletion |
| **Live HSTS (NEW)** | ✅ 1-yr | Enforced by GitHub Pages infra |

---

## § Patch log (empty)

`/audit` does not invoke patch-agent (portfolio audit skill: *"patch-agent on standby — not activated unless P0s found and confirmed"*). No P0s found this run. Use `/orchestrate` if you want automated patching.

---

## § Methodology notes

1. **Baseline file**: `docs/audits/2026-04-16.md` (prior state captured in commit `2723743`'s copy, updated in `9684d83` release merge). Prior version recoverable via git history.
2. **Agent isolation**: all three subagents ran in isolated worktrees branched off `revamp` HEAD (`dcaf1d9`). Per session history, this auto-branch-from-HEAD sometimes lands on an older commit (`118b657`); agents instructed to reset to briefed base if needed. No incidents this run.
3. **Composite arithmetic**: audit-agent's stated 7.6 matches hand-computed 7.55 from declared 20/15/20/15/30 weights. ✓
4. **Conservative scoring acknowledged**: orchestrator notes the swarm's completion likely warrants a 7.8–8.0 on tighter calibration, but the agent's 7.6 stands as the returned value (orchestrator integrity over result-shopping).
5. **No patch-agent invoked** per portfolio `/audit` skill contract.

---

## § Next-phase recommendation

**Engineering work is complete and shipped.** Remaining leverage is content, per ADR-005.

Single next recommended action, ranked:

1. **Regenerate `docs/ARCHITECTURE.md` from current source** (15 min, closes the only new P1 this run). Pairs well with Phase-2 hardening: add a CI check that fails on claimed-file-not-present drift.
2. **Commit or gitignore `CLAUDE.sysadmin-portfolio.md`** (owner decision, 2 min).
3. **Begin ADR-005 content work** — cert honesty, demo placeholder elimination, work-history quantification, sitemap reconciliation. The engineering substrate can now carry content confidently.

Engineering-layer work should pause. The 8.0+ composite score shift from here is content, not code.
